//exportar una modulo así significa que el código va a ser ejecutado
//todo completo, ya que se esta invocando a la funcion desde aqui mismo


const num1 = 5;
const num2 = 10

function addValues(){
	console.log(`the sum is : ${num1 + num2}`)
}

addValues()